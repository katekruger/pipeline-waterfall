name: 'pipeline_waterfall_integration_tests'
version: '0.1.0'
config-version: 2

profile: 'pipeline_waterfall_integration_tests'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

clean-targets:
  - "target"
  - "dbt_packages"
